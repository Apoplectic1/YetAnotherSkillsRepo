# 2026-07-26 — b4-portfolio-domain RED/GREEN results

**Charter:** dated validation record for `b4-portfolio-domain` (SETUP B4′ + MAINTAIN M15).
Fixture: `harness/reefstack-fixture/` (new portfolio-container family; catalog
`harness/catalog-reefstack.md`). Deriving evidence (poisoned for validation): the Astronomy
container probe (`docs/2026-07-26-b4-portfolio-probe.md`) and the TSM glossary loop. All
reps Sonnet-high via Workflow; SETUP reps plan-only/read-only (Explore); context-leak
disclosure per VERIFICATION rule 4.

## RED — current deployed text
- **SETUP (PC1):** the strongest non-determinism evidence of the week — across 4 container
  reps (2 Astronomy probe + 2 fixture), current text produced **three different
  resolutions** of the S7↔B4 collision: cross-repo prose → root ROADMAP; kept-in-router as
  "gotcha"; and (both fixture reps) **create a root DOMAIN while openly declaring it "a
  narrow exception to B4"** — compliant agents forced to choose which rule to break.
  A rule contradiction, not a discipline failure; both fixture reps honored the owner rule
  and flagged the loop unprompted.
- **MAINTAIN (PL1):** on-disk rep graduated the portfolio truth **into the container
  router's Data-contracts section** (router-stuffing), edited parent + own DOMAIN from a
  sub-project run, and **half-broke the planted loop** (repointed its side, stranded the
  sibling's — flagged that itself). No hold, no ask.

## GREEN — candidate B4′/M15 injected (5 reps, all pass)
- **SETUP 2/2 (PC1–PC3):** portfolio DOMAIN planned as sanctioned behavior (no exception
  framing): config.db disambiguation + quiet-window convention move there; readings.db
  stays owner-side at `PulseMonitor/SCHEMA.md` with pointers down (owner rule 2/2); router
  keeps one-line gotchas; no extra set; thin status ROADMAP recognized as compliant.
- **Control (PC4):** SETUP at `ReefDash/` alone — correctly classified first-party project,
  planned in-repo alignment only (its missing VERIFICATION.md), **no portfolio DOMAIN
  anywhere** (no over-fire).
- **MAINTAIN on-disk (PL1):** textbook M15 hold — dated report + one ROADMAP open-line
  naming the placement decision; container root + siblings **byte-untouched** (disk-
  verified, 0 diff lines outside ReefDash); NOTEBOOK why/when intact; loop folded into
  evidence, not mutated; the healthy in-repo graduate (ring-buffer → own ARCHITECTURE)
  still applied — no over-holding.
- **PL2 probe:** portfolio DOMAIN present → `placement: portfolio-domain`, M9 content-test
  run first, why/when preserved.

## Shipped
SETUP **B4** amended in place (router + at most two thin portfolio docs; owner rule;
one-way pointers; S7 container destination) + MAINTAIN **M15** (portfolio targeting;
flag-and-ask on M13 rails; improvisation paths named). Change record:
`openspec/changes/archive/2026-07-26-b4-portfolio-domain`.

## Notable
- RED's best evidence was reps *knowingly* breaking B4 to do the right thing — a rule that
  criminalizes the correct action is the purest RED form yet observed.
- The maintain RED rep's router-stuffing shaped M15's wording: all three improvisation
  paths (container router, sibling docs, new cross-repo pointer) are named explicitly.
- Consumer adoption queued (post-deploy, own sessions): Astronomy container portfolio
  DOMAIN.md + Data-flow-hubs relocation; TSM glossary loop → one-way pointers.
