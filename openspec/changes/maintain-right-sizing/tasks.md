# maintain-right-sizing — tasks

## 1. Fixture — PR plant cells (TidePool)

- [ ] 1.1 PR1: add a ROADMAP Now/Next item whose completion is visible in the journal
      (closed-item-in-open-list, M4 class); PR2: confirm/plant a spent journal record fully
      superseded by a reference doc (M3 class)
- [ ] 1.2 Catalog PR1/PR2 expected RED/GREEN in `harness/catalog-tidepool.md`

## 2. RED — current text/description

- [ ] 2.1 Description probe-RED (2 fresh-context reps): low-yield scenario (sweep 2 h ago,
      1 archived change since) with the CURRENT description — does the agent recommend
      running? Capture verbatim
- [ ] 2.2 M16 RED: 1 on-disk maintain rep on the PR-planted fixture — are PR1/PR2 found
      without a forced pass? does the output account for net reference growth? Reset after
- [ ] 2.3 Classify failure forms per half; no-failure → status-note that half

## 3. Candidate text

- [ ] 3.1 New description (triggers only) + M16 (required prune pass + REQUIRED report
      accounting slot + net-delta line); word delta ≤ ~70; IDs append-only

## 4. GREEN

- [ ] 4.1 Description probe-GREEN: same low-yield scenario with the new description →
      declines with trigger citation; real-trigger scenario → still fires
- [ ] 4.2 M16 GREEN (if that half REDs): 1 on-disk rep — PR cells flagged, accounting slot
      + net-delta present in the report; reset after
- [ ] 4.3 Regression: healthy graduates still apply (no over-pruning of genuine keeps)

## 5. Ship

- [ ] 5.1 Apply description + M16 (as gated) on dev
- [ ] 5.2 Dated RED/GREEN record + design-doc provenance + ROADMAP shipped line + NOTEBOOK
      disposition (right-sizing candidates item)
- [ ] 5.3 Archive (--yes, judge by output text); merge dev → main; deploy.sh; push
