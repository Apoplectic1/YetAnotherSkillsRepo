# readme-audience-rule — tasks

## 1. Verify implementation (already landed, commit 995175d)

- [x] 1.1 README.md conforms: Why section plain-language (no benchmark links), workshop
      confined to the repo-layout paragraph, fixtures labeled fictional/not-templates
- [x] 1.2 RELEASING.md § Public mirror carries the README audience rule (+ the SKILL.md
      never-simplified counterpart)

## 2. Sync + close

- [x] 2.1 Validate the change (`openspec validate readme-audience-rule`)
- [x] 2.2 Archive with `--yes`, judge by output text; commit; merge `dev` → `main`; push
      mirror (no deploy needed — no skill text changed)
