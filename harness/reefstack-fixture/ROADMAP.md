# ROADMAP.md — ReefStack portfolio status

**Charter:** thin portfolio status board — current thrust + per-repo state. Volatile status
lives here, deliberately not in the auto-loaded CLAUDE.md. Authoritative detail lives in
each repo's own ROADMAP.md.

- **Current thrust:** event-feed quality (ReefDash) + dosing safety (DoseKeeper).
- PulseMonitor — stable; pH drift compensation next. Detail: `PulseMonitor/ROADMAP.md`.
- ReefDash — event-feed filtering next. Detail: `ReefDash/ROADMAP.md`.
- DoseKeeper — two-part dosing next; portfolio settings service unbuilt. Detail:
  `DoseKeeper/ROADMAP.md`.
